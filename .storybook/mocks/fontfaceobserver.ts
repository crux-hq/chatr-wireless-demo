export default class FontFaceObserver {
  constructor(_family: string, _options?: unknown) {}

  load(_testString?: string, _timeout?: number) {
    return Promise.resolve();
  }

  loadAll(_testString?: string, _timeout?: number) {
    return Promise.resolve();
  }
}
